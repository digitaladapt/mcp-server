# Runtime dependencies (also defined in pyproject.toml).
# This file is kept for simple `pip install -r` without editable install.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pyyaml>=6.0
httpx>=0.27
caldav>=1.3
icalendar>=6.0
